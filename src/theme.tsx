import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#673AB7',
    },
    secondary: {
      main: '#512DA8',
    },
    success: {
      main: '#2196F3',
    },
  },
});

export default theme;
